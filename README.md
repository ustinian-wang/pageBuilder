# Page Builder

一个基于 Next.js 15 的可视化 PC 端页面构建器，支持拖拽搭建页面并生成可维护的 Vue 组件代码。

## 功能特性

- 🎨 可视化拖拽编辑器
- 📦 丰富的组件库（按钮、输入框、卡片、布局等）
- ⚙️ 组件属性配置面板
- 👁️ 实时预览
- 💾 保存页面配置
- 📝 生成 Vue 2 组件代码（可维护、可扩展、一键复制）

## 技术栈

- **Next.js 15** - React 框架
- **TypeScript** - 类型安全
- **Tailwind CSS** - 样式框架
- **@dnd-kit** - 拖拽功能
- **LowDB** - 轻量级 JSON 数据库
- **bcryptjs** - 密码加密
- **JWT** - 身份认证

## 快速开始

### 安装依赖

```bash
yarn install
```

### 初始化数据库

首次运行项目前，需要初始化数据库：

```bash
yarn init-db
```

### 启动开发服务器

```bash
yarn dev
```

访问 [http://localhost:3000](http://localhost:3000)

## 项目结构

```
pageBuilder/
├── data/              # 数据库文件
│   ├── db.json       # 主数据库（页面配置）
│   └── pages/        # 生成的 Vue 组件代码
├── scripts/          # 脚本文件
│   └── init-db.ts    # 数据库初始化脚本
├── src/
│   ├── app/          # Next.js App Router
│   │   ├── api/      # API 路由
│   │   │   ├── pages/ # 页面配置 API
│   │   │   └── generate/ # 代码生成 API
│   │   └── builder/  # 编辑器页面
│   ├── components/   # React 组件
│   │   ├── builder/  # 编辑器相关组件
│   │   └── elements/ # 可拖拽的元素组件
│   ├── lib/          # 工具函数
│   │   ├── generator/ # Vue 代码生成器
│   │   └── types.ts  # 类型定义
│   └── hooks/        # 自定义 Hooks
└── public/           # 静态资源
```

## License

MIT

